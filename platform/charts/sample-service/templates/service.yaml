apiVersion: v1
kind: Service
metadata:
  name: sample-service
  labels:
    app: sample-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: sample-service
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
