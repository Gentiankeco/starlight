apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hello-service
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Gentiankeco/starlight.git
    targetRevision: main
    path: platform/charts/hello-service
  destination:
    server: https://kubernetes.default.svc
    namespace: hello-service
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
      - CreateNamespace=true
