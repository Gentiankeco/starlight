replicaCount: 2

containerPort: 8080

image:
  repository: ghcr.io/gentiankeco/starlight-demo-app
  tag: "latest"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 256Mi
